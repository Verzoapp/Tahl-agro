query GetGeoCorpById($geoCorpId: String!){
  getGeoCorpById(geoCorpId:$geoCorpId){
    id
    name
    archived
    createdAt
    landGroup{
      name
      id
    }
    geographicArea{
      name
      id
    }
    farmLots{
      name
      id
    }
    
  }
}